{
  "name": "eastern-sierra-trip",
  "version": "1.0.0",
  "private": true,
  "description": "Interactive trip map for Eastern Sierra — May 2026",
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "dev": "npx serve . -p 8080 --no-clipboard"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  }
}
